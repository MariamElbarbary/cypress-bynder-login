{
  "name": "bynder-ui-tests",
  "version": "1.0.0",
  "description": "Ui tests with cypress for the bynder website",
  "main": "index.js",
  "scripts": {
    "test": "cypress run --config-file=cypress.config.js",
    "cypress:open": "cypress open"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^12.0.2"
  },
  "devDependencies": {
    "eslint-plugin-cypress": "^2.12.1"
  }
}
